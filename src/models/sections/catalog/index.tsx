import { ICatalogProps } from "./types";

import styles from "./styles.module.scss";

export const Catalog = ({}: ICatalogProps): JSX.Element => {
  return <section className={styles.container}></section>;
};
